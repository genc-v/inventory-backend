# Copy to .env for local (non-Docker) runs. docker-compose has sane defaults.
NODE_ENV=development
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=inventory

# Auth
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=1h
